# Local Postgres (replace USER with your OS username if needed):
DATABASE_URL=postgresql://USER@localhost:5432/dispute_portal
#
# Docker Compose Postgres (host port 5433 → container 5432):
# DATABASE_URL=postgresql://postgres:password@localhost:5433/dispute_portal

PORT=3001
CORS_ORIGIN=http://localhost:5173
