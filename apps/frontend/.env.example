# Set to "false" to disable the MSW mock server and hit the real backend
# (requires `npm run dev:backend` running, proxied via vite.config.ts)
VITE_USE_MSW=true

# Only needed if the backend isn't on the default http://localhost:3001
# VITE_API_URL=http://localhost:3001
